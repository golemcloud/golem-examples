{
  "description": "A simple stateless Golem function written in C# with no dependencies on external services",
  "requiresAdapter": true,
  "exclude": [
    "bin",
    "obj",
    "component_name.wasm"
  ]
}